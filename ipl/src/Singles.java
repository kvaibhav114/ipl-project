import java.util.*;

public class Singles {
    void fetchSingles(Map<Integer, Match> matchMap, Map<Integer, List<Deliveries>> deliveriesMap){
        Map<Integer, Map<String, Map<String, Integer>>> resultMap = new HashMap<>();
        for(int id : matchMap.keySet()){
            for(Deliveries d : deliveriesMap.get(id)){

            }
        }
    }
}
